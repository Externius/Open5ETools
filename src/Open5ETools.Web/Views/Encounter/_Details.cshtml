@model Open5ETools.Web.Models.Encounter.EncounterViewModel

<div class="row row-cols-1 row-cols-md-4 g-4 mt-2">
    @foreach (var monster in Model.Monsters)
    {
        <div class="card text-bg-light mx-5 my-1">
            <div class="row g-0">
                <div class="col-md-2 align-self-center mt-2">
                    <button type="button" class="btn btn-link btn-lg" id="monsterDetail" data-monster-id="@monster.Id"
                            title="Monster details">
                        <i class="fa-solid fa-dice-d20 fa-2xl"></i>
                    </button>
                </div>
                <div class="col-md-10">
                    <div class="card-body text-center">
                        <h5 class="card-title">
                            @monster.Monster.Count x @monster.Monster.Name
                        </h5>
                        <h6 class="card-subtitle mb-2 text-muted"> @monster.Monster.Type
                            (@monster.Monster.Size)</h6>
                        <p class="card-text">
                            @Resources.Common.Difficulty: @monster.Monster.Difficulty
                        </p>
                        <p class="card-text">
                            (@monster.Monster.Xp @Resources.Encounter.Xp)
                        </p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
